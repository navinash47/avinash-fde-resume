name: Assemble PDF
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PDF assembly disabled; using Avinash_Nandyala_FDE.pdf.b64"
